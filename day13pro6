#include <stdio.h>

int main() {
    int number;
    scanf("%d", &number);

    int tens = number / 10;
    int ones = number % 10;

    int sum = tens + ones;

    printf("%d\n", sum);

    return 0;
}
